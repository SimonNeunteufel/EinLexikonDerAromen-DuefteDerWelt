:root {
    --t: #f7fafc;
    --bg: rgba(15, 18, 22, .65);
    --b: #ffffff30;
}

* { box-sizing: border-box; }

body {
    margin: 0;
    font-family: system-ui, Segoe UI, Roboto, Arial, sans-serif;
    color: var(--t);
    background: #0f1216;
    background-size: cover;
    background-attachment: fixed;
}

.header {
    position: sticky;
    top: 0;
    z-index: 5;
    display: flex;
    justify-content: space-between;
    gap: 10px;
    align-items: center;
    padding: 10px;
    background: #0008;
    border-bottom: 1px solid var(--b);
}

.brand {
    font-weight: 900;
    font-size: clamp(22px, 3.2vw, 36px);
    text-shadow: 0 0 8px #fff8;
}

.nav a {
    color: #eaf6ff;
    text-decoration: none;
    padding: 6px 10px;
    border-radius: 8px;
}

.nav a:hover { background: #ffffff22; }

.page {
    max-width: 1200px;
    margin: 16px auto;
    padding: 16px;
    border: 1px solid var(--b);
    border-radius: 12px;
    background: var(--bg);
}

/* --- FORM ELEMENT FIXES --- */
input, select {
    background: #0b0f14;
    border: 1px solid var(--b);
    color: #f7fafc;
    border-radius: 10px;
    padding: 10px 12px;
    min-width: 220px;
}

/* Spezifischer Fix für Checkbox-Sichtbarkeit auf Desktop (Edge/Chrome) */
input[type="checkbox"] {
    appearance: checkbox !important;
    -webkit-appearance: checkbox !important;
    min-width: 18px;
    width: 18px;
    height: 18px;
    cursor: pointer;
    vertical-align: middle;
    margin: 0;
}

.btn {
    background: #fff;
    color: #0b0f14;
    border: 1px solid #0003;
    border-radius: 10px;
    padding: 10px 14px;
    cursor: pointer;
}

/* --- TABELLEN STYLES --- */
.table-wrap {
    background: #fff;
    color: #0b0f14;
    border-radius: 12px;
    border: 1px solid #0002;
    overflow: hidden; /* Stellt sicher, dass border-radius wirkt */
}

.table-body {
    height: calc(100vh - 280px);
    overflow-y: auto;
}

table {
    width: 100%;
    border-collapse: collapse;
    table-layout: fixed; /* Wichtig für exakte Spaltensteuerung */
}

th, td {
    padding: 10px 12px;
    border-bottom: 1px solid #eef2f7;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
}

/* Fix für die Checkbox-Spalte: Darf nicht auf 0px kollabieren */
th:first-child, td:first-child {
    width: 50px;
    min-width: 50px;
    text-align: center;
    overflow: visible; /* Checkbox wird sonst abgeschnitten */
}

thead th {
    position: sticky;
    top: 0;
    background: #f8fafc;
    z-index: 2;
}

/* --- RESTLICHE ELEMENTE --- */
.badges { display: flex; gap: 8px; flex-wrap: wrap; margin: 8px 0; }
.badge {
    background: #eef3ff;
    border: 1px solid #dae6ff;
    border-radius: 999px;
    color: #0b0f14;
    padding: 4px 10px;
}

.footer-note {
    margin-top: 10px;
    display: flex;
    justify-content: space-between;
    opacity: .9;
}